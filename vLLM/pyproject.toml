[project]
name = "vllm-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ray[default]>=2.55.1",
    "tokenizers>=0.21.1,<0.22",
    "transformers==4.53.3",
    "vllm==0.10.0",
]
