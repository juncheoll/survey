# Half-Quadratic Quantization (HQQ)

The following code is modified from the original code in the [HQQ](https://github.com/mobiusml/hqq) library. See their [blog post](https://mobiusml.github.io/hqq_blog/) for more information.

----

HQQ is a fast and accurate model quantizer that skips the need for calibration data. Quantize the largest models, without calibration data, in just a few minutes at most 🚀.

### Installation
```bash
#latest stable version
pip install hqq
pip install gemlite
```